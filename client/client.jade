head
    title DAO
    meta(name='viewport', content='user-scalable=no, initial-scale=1.0, maximum-scale=1.0, width=device-width')
    meta(name="theme-color", content="#ffffff")
    link(rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Ubuntu")

body
    +home


template(name='home')
    +nav
    .ui.stackable.padded.grid
        .centered.row
            if delta
                .ui.icon.button.delete_delta
                    i.remove.link.icon
                .ui.icon.button.print_delta
                    i.code.link.icon
                .ui.icon.button.recalc
                    i.refresh.link.icon
            else
                .ui.icon.button.create_delta
                    i.plus.link.big.icon
        .two.column.row
            .column
                each delta.facets
                    +facet
            .column
                each delta.results
                    +result
    +footer


template(name='result')
    .ui.segment
        .ui.list
            each keys
                .item 
                    |#{this}
                    strong #{value}


template(name='facet')
    .ui.header #{key}
    // each filters
    //     .unselect.ui.blue.button #{this}
    
    each res
        .ui.toggle_selection.margined.compact.button(class=toggle_value_class)
            strong #{name}
            small #{count}
            
            
            
template(name='footer')
    .ui.bottom.fixed.inverted.menu
        .ui.center.aligned.inverted.grey.small.header
            .item
                i.copyright.outline.icon
                | 2018 Data Analytics Organization



template(name='role_switcher')
    each role_docs
        .change_role.ui.button(class=role_button_class)
            if icon
                i.icon
                    +icon name=icon
            |#{title}
            