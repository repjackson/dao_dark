template(name='notify_button')
    if currentUser
        .ui.header
            +icon name='appointment-reminders'
            |Notify Someone
        // .ui.fluid.input
        //     +inputAutocomplete settings=recipient_select_settings id="recipient_select" class="" autocomplete="off" placeholder='Lookup...'
    .ui.list
        each notified_users
            .item
                img.ui.avatar.image(src="{{c.url profile.image_id width=100 height=100 gravity='face' crop='fill'}}")
                .content
                    .header #{name}
                    .meta #{username}
                .right.floated.content
                    i.remove_notified_user.large.link.red.remove.icon

template(name='checkin')
    unless healthclub_checkedin
        .ui.button.checkin(title='Check In')
            i.sign.in.icon
            |Sign In
    if healthclub_checkedin
        .ui.button.checkout(title='Sign Out')
            i.sign.out.large.icon
            |Sign Out


template(name='view_section')
    // a.ui.icon.button(href="/schema/#{_id}/edit")
    //     i.pencil.icon
    //     |Configure Schema
    a.ui.view_section.circular.icon.large.basic.button(href="/t/#{ct.slug}/s/#{slug}" title='Select Schema')
        i.sitemap.large.icon
        // +icolor name='back-sorting' classes='ui mini inline image'
        
        
template(name='page_view')
    a.ui.circular.basic.icon.button(href="/t/#{ct.slug}/p/#{slug}")
        // i.eye.icon
        +icolor name='visible' classes='ui mini inline image'
        | Page View
        
        
template(name='enter_tribe')
    a.ui.enter_tribe.circular.basic.icon.button(href="/t/#{slug}" title="Enter #{title} Tribe")
        +icolor name='fire' classes='ui mini inline image'
