template(name='edit')
    with doc
        .ui.segment.container
            .ui.button.print print
            each _keys
                .ui.label #{this}                
            .content
                .ui.labeled.input
                    .ui.label new key
                    input.new_key(type='text' placeholder='new key name')
            .content
                each _keys
                    +field
                    
                    
                    
template(name='field')
    .ui.segment
        .ui.header key:
            strong #{this}
        .ui.labeled.input
            .ui.label label: #{meta.label}
            input.change_label(type='text' placeholder='label' value=meta.label)
        .ui.labeled.input
            .ui.label value
            input(type='text' value=key_value)
        .ui.icon.button.remove_field
            i.remove.icon
                        
                
        

